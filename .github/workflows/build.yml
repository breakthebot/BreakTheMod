name: Build Check

on:
  push:
    branches: [ "*" ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew clean build --no-daemon -Prelease -Pgithub

      - name: Shrink jar
        run: ./gradlew shrinkJar

      - name: Rename shrunk jar
        run: mv build/libs/*-min.jar build/libs/breakthemod.jar

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jars
          path: build/libs/breakthemod.jar